
@{
    ViewBag.Title = "WelcomeAdmin";
}
<div class="jumbotron">
    <h1>Travel and Book Products</h1>
    <p><a href="https://asp.net" class="btn btn-primary btn-lg">Learn more &raquo;</a></p>
</div>
<h2>Choose a Product</h2>
<div class="row">
    <div class="col-md-4">
        @using (Html.BeginForm("Activity", "Home", FormMethod.Post))
        {
            <button class="btn btn-success">Activity</button>
        }
        </div>
    <div class="col-md-4">
        @using (Html.BeginForm("Air", "Home", FormMethod.Post))
        {
            <button class="btn btn-success">Air</button>
        }
        </div>

    <div class="col-md-4">
        @using (Html.BeginForm("Login", "Home", FormMethod.Post))
        {
            <button class="btn btn-success">Car</button>
        }
        </div>
    <div class="col-md-4">
        @using (Html.BeginForm("Login", "Home", FormMethod.Post))
        {
            <button class="btn btn-success">Hotel</button>
        }
        </div>
</div>

